import { {{properCase name}}Action } from "./actions";
import { {{constantCase name}}_DATA, {{constantCase name}}_ERROR, {{constantCase name}}_FETCH } from "./constants";

export interface {{properCase name}} {
  {{properCase name}}: string;
}

export interface {{properCase name}}State {
  error?: string;
  loading: boolean;
  success: boolean;
  // {{properCase name}}: {{properCase name}}
}

export const initialState:  {{properCase name}}State = {
  loading: false,
  success: false,
};

export const {{camelCase name}}Reducer = (state = initialState, action:  {{properCase name}}Action) => {
  switch (action.type) {
    case {{constantCase name}}_FETCH:
      return {
        ...state,
        laoding: true,
        success: false,
      };
    case {{constantCase name}}_DATA:
      return {
        ...state,
        laoding: false,
        success: true,
      };
    case {{constantCase name}}_ERROR:
      return {
        ...state,
        laoding: false,
        success: false,
        error: action.error,
      };
    default:
      return state;
  }
};
