import { {{constantCase name}}_DATA, {{constantCase name}}_ERROR, {{constantCase name}}_FETCH } from "./constants";


export interface {{properCase name}}Fetch {
  type: typeof {{constantCase name}}_FETCH;
}

export interface {{properCase name}}Error {
  type: typeof {{constantCase name}}_ERROR;
  error: string;
}

export interface {{properCase name}}Data {
  type: typeof {{constantCase name}}_DATA;
}

export type {{properCase name}}Action = {{properCase name}}Fetch | {{properCase name}}Data | {{properCase name}}Error;

export const {{camelCase name}}Fetch = (): {{properCase name}}Fetch => ({
  type: {{constantCase name}}_FETCH,
});

export const {{camelCase name}}Data = (): {{properCase name}}Data => ({
  type: {{constantCase name}}_DATA,
});

export const {{camelCase name}}Error = (error: string): {{properCase name}}Error => ({
  type: {{constantCase name}}_ERROR,
  error,
});
