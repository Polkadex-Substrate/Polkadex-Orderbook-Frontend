import { RootState } from "../../";

export const select{{properCase name}}Success = (state: RootState): boolean =>
  state.public.{{properCase name}}.success;
export const select{{properCase name}}Error = (state: RootState): string =>
  state.public{{properCase name}}.error;
export const select{{properCase name}}Loading = (state: RootState): boolean =>
  state.public.{{properCase name}}.loading;
