import * as S from "./styles";

// import { useTradingView } from "@polkadex/orderbook/v2/hooks";

export const TradingView = () => {
  // const { id } = useTradingView();

  return (
    <S.Wrapper
      // id={id}
      className="pg-trading-chart"
    />
  );
};
